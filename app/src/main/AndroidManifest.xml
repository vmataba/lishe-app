<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emmanuel.lisheapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DashboardActivity"
            android:theme="@style/LargeActionBar" />
        <activity
            android:name=".LisheListActivity"
            android:theme="@style/LargeActionBar">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".RegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchResultsActivity">
            ...
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            ...
        </activity>
        <activity android:name=".SwaliActivity" />
        <activity android:name=".MaoniActivity" />
        <activity android:name=".UserAccountActivity" />
        <activity android:name=".MsaadaActivity" />
        <activity android:name=".LisheContentsActivity" />
        <activity android:name=".MaswaliYanguActivity" />
        <activity android:name=".ReadLisheActivity" />
        <activity android:name=".UpdateAccountActivity"></activity>
    </application>

</manifest>